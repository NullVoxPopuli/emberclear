<div class='navbar is-fixed-top has-shadow {{if isChat "is-primary"}}'>
  <div class='container {{if isChat "is-fluid"}}'>
    <div class='navbar-brand'>
      {{#link-to 'application' class=(concat 'navbar-item ' (if isChat textColor))}}
        {{t 'appname'}}
      {{/link-to}}



      <h1 class='navbar-item'>Chat: <CurrentChatName /></h1>

      <HamburgerButton
        @onClick={{action toggleTouchMenu}}
        @isActive={{isTouchMenuVisible}} />
    </div>


    <div class="navbar-menu transition-all {{if isTouchMenuVisible 'is-active'}}">
      <div class="navbar-end">

        {{#if isLoggedIn}}
          <a class='navbar-item'
            {{action (queue
              (action toggleTouchMenu)
              (transition-to 'chat')
            )}}>
            {{t 'routes.chat'}}
          </a>

          <UserDropMenu
            class='navbar-item has-dropdown'
            @onClose={{action toggleTouchMenu}}
            />

        {{else}}
          <a class='navbar-item'
            {{action (queue
              (action toggleTouchMenu)
              (transition-to 'login')
            )}}>
            {{t 'routes.login'}}
          </a>

          <a class='navbar-item'
            {{action (queue
              (action toggleTouchMenu)
              (transition-to 'setup')
            )}}>
            {{t 'routes.createNewUser'}}
          </a>
        {{/if}}

      </div>
    </div>
  </div>
</div>
