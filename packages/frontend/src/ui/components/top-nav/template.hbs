<div class='navbar has-shadow'>
    <div class='container'>
        <div class='navbar-brand'>
            {{#link-to 'application' class='navbar-item'}}
              {{t 'appname'}}
            {{/link-to}}
            <a
              role='button'
              class="navbar-burger burger {{touchMenuClasses}}"
              aria-label="menu" aria-expanded="false"
              {{action 'toggleTouchMenu'}}>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
            </a>
        </div>
        <div class="navbar-menu {{touchMenuClasses}}">
            <div class="navbar-end">
                {{#link-to 'chat' class='navbar-item'}}
                    {{t 'routes.chat'}}
                {{/link-to}}

                {{#if hasName}}
                  {{#link-to 'settings' class='navbar-item'}}
                      <span class='icon'>
                          <i class='fas fa-user-circle'></i>
                      </span>
                      {{!-- {{t 'routes.profile'}} --}}
                      {{name}}
                  {{/link-to}}
                {{/if}}
            </div>
        </div>
    </div>
</div>
