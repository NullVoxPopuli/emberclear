<ModalContainer as |container|>
  <a
    class={{@linkClass}}
    {{action container.toggle}}>Add Friend</a>

  {{to-elsewhere
    named='modals'
    send=(component 'add-modal'
      isActive=container.isActive
      close=(action (mut container.isActive) false)
    )
  }}
</ModalContainer>
