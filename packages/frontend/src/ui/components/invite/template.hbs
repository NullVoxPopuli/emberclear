<ModalContainer as |container|>
  <button class='button' {{action container.toggle}}>Invite</button>

  <Modal @isActive={{container.isActive}} @close={{action container.toggle}}>
    <pre>
      <code>
        {}
      </code>
    </pre>
  </Modal>
</ModalContainer>
