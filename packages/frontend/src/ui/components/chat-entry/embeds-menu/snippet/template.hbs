<Modal @isActive={{@isActive}} @close={{@close}}>
  <h5 class='is-size-5 m-b-md'>
    Send Code or Snippet to <strong>{{@sendTo.name}}</strong>
  </h5>

  <div class='flex-row m-b-md m-r-sm'>
    {{input
      class='input'
      value=title
      placeholder='Title (optional)'}}

    <div class='select m-l-sm'>
      <select onchange={{action (mut language) value="target.value"}}>
        {{#each languages as |availableLanguage|}}
          <option value={{availableLanguage}}>{{availableLanguage}}</option>
        {{/each}}
      </select>
    </div>
  </div>

  {{textarea
    autofocus
    role='snippet-entry'
    class='input transition-all m-b-md snippet-entry'
    value=text
  }}

  <div class='flex-row justify-content-end'>
    <button class='button m-r-sm' {{action @close}}>Cancel</button>
    <button class='button is-primary' {{action sendMessage}}>Send Snippet</button>
  </div>
</Modal>
