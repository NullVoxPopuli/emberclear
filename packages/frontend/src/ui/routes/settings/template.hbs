<div data-test-settings-wrapper class='section'>
  <div class='container'>
    <h1 class='title'>Settings</h1>
    <div class='content'>

      <form {{action 'save' on='submit'}}>

        <Field @label='Name'>
          {{input data-test-name-field class='input' value=name}}
        </Field>

        <div class='flex justify-content-end'>
          <input data-test-save class='button' type='submit' value='Save'>
        </div>
      </form>

      <button data-test-show-private-key-toggle class='button' {{action (mut showPrivateKey) (not showPrivateKey)}}>
        {{if showPrivateKey 'Hide' 'Show'}} private key
      </button>

      {{#if showPrivateKey}}
        <MnemonicDisplay @privateKey={{identity.privateKey}} />
      {{/if}}

      <a {{action downloadSettings}} class='button'>
        Download Settings
      </a>

      <hr>

      <h2 class='is-size-4'>Danger Zone</h2>

      <button
        data-test-delete-messages
        disabled={{messagesDeleted}}
        class='button is-danger is-outlined'
        {{action 'deleteMessages'}}>

        <span class='icon is-small'>
          {{fa-icon 'times'}}
        </span>
        <span>Delete Messages</span>
      </button>
    </div>
  </div>

</div>
