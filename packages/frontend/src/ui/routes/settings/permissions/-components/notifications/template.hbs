<div data-test-notifications>
  <label class='label'>{{t 'compatibility.notifications'}}</label>

  {{#if this.isEnabled}}
    {{t 'status.enabled'}}
  {{else}}
    <button {{action this.askNotifications}} class='button'>
      {{t 'buttons.enabled'}}
    </button>
  {{/if}}
</div>

