<div class='container'>
  <div class='content'>
    <h1>Contacts ({{model.length}})</h1>

    {{log model.length}}
    {{#each model as |identity|}}
      {{identity.name}} - {{identity.publicKeyAsHex}}
    {{/each}}
  </div>
</div>
