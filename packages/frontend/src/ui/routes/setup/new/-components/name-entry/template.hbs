<form data-test-name-form {{action this.createIdentity on='submit'}}>

  <div class="content">
    {{input
      data-test-name-field
      class='input is-large'
      type='text'
      placeholder=(t 'ui.setup.nameLabel')
      value=this.name
    }}
  </div>

  {{#link-to 'login'}}
    Login instead
  {{/link-to}}

  <input
    data-test-next
    type='submit'
    class='button is-pulled-right'
    disabled={{this.nameIsBlank}}
    value={{t 'buttons.next'}}
    {{action this.createIdentity}} />

</form>
