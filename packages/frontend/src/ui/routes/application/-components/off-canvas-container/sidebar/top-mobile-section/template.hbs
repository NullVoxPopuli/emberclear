{{#if this.isLoggedIn}}
  <span class='is-size-4'>{{this.name}}</span>

  <ul class='menu-list'>
    <li>
      <a {{action (queue
        (action this.closeSidebar)
        (transition-to 'contacts'))
      }}>
        <span class='icon'>{{fa-icon 'address-book' prefix='fas'}}</span>
        <span>{{t 'routes.contacts'}}</span>
      </a>
    </li>

    <li>
      <a {{action (queue
        (action this.closeSidebar)
        (transition-to 'settings'))
      }}>
        <span class='icon'>{{fa-icon 'sliders-h'}}</span>
        <span>{{t 'routes.settings'}}</span>
      </a>
    </li>

  </ul>

  <hr class='divider m-b-sm m-t-sm'>
{{/if}}
