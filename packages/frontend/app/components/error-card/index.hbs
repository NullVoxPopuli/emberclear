<div class='card'>

  <header>{{t 'errors.genericTitle'}}</header>

  <p>
    {{@message}}
  </p>

  <footer class='cta'>
    {{#if @retry}}
      <button type='button' class='button-secondary' {{on 'click' @retry}}>
        {{t 'buttons.retry'}}
      </button>
    {{/if}}
  </footer>
</div>