<table data-test-contacts>
  <thead>
    <tr>
      <th>{{t 'models.identity.name'}}</th>
      <th>{{t 'models.identity.publicKey'}}</th>
      <th>{{t 'models.identity.verification'}}</th>
      <th></th>
    </tr>
  </thead>
  <tbody>

    {{#each this.contacts key='id' as |contact|}}
      <tr data-test-contact-row>
        <td>
          {{contact.name}}
        </td>
        <td>
          {{first-8 contact.publicKeyAsHex}}...
        </td>
        <td>
          <LinkTo 
            class='button'
            @route='contacts.verify-friend' 
            @model={{contact.id}} 
          >
            {{#if contact.verification}}
              {{t 'models.identity.verified'}}
            {{else}}
              {{t 'models.identity.unverified'}}
            {{/if}}
          </LinkTo>
        </td>
        <td class='flex justify-content-end'>
          <button
            type='button'
            class='button-danger'
            {{on 'click' (fn this.remove contact)}}
          >
            {{t 'buttons.remove'}}
          </button>
        </td>
      </tr>
    {{/each}}

    {{#if (eq this.contacts.length 0)}}
      <tr>
        <td colspan='3'>
          {{t 'ui.contacts.noContacts'}}
        </td>
      </tr>
    {{/if}}

  </tbody>
</table>
