<div>
  <form {{on 'submit' this.save}}>

    <Field @label='Name' as |id|>
      <Input
        data-test-name-field
        id={{id}}
        @value={{this.name}}
      />

    </Field>

    <div class='cta'>
      <input
        data-test-save
        class='button'
        type='submit'
        value={{t 'buttons.save' }}
      >
    </div>

  </form>

  <hr>

  <button {{on 'click' this.downloadSettings}} class='button'>
    {{t 'ui.settings.download' }}
  </button>

  <button {{on 'click' this.transfer.start}}>
    {{t 'ui.settings.copyProfileToDevice'}}
  </button>
</div>
