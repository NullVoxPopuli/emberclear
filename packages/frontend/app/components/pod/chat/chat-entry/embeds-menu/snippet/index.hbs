<Modal
  data-test-embeds-snippet-modal
  @isActive={{@isActive}}
  @close={{@close}}
>
  <h5>
    {{t 'ui.chat.codeModal.title'}} <strong>{{@sendTo.name}}</strong>
  </h5>

  <hr>

  <div local-class='snippet-title-bar'>
    <Input
      placeholder='Title (optional)'
      @value={{this.title}}
    />

    <div>
      <select onchange={{action (mut this.language) value='target.value'}}>
        {{#each this.languages as |availableLanguage|}}
          <option value={{availableLanguage}}>{{availableLanguage}}</option>
        {{/each}}
      </select>
    </div>
  </div>

  {{textarea
    autofocus=true
    local-class='snippet-entry'
    value=this.text
  }}

  <div class='button-group right-aligned'>
    <button data-test-cancel class='button-secondary' {{on 'click' @close}}>
      {{t 'buttons.cancel'}}
    </button>
    <button data-test-submit {{on 'click' this.sendMessage}}>
      {{t 'buttons.sendSnippet'}}
    </button>
  </div>
</Modal>

