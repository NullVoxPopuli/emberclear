<div class='input-field' ...attributes>

  {{#if @label}}
    <label
      class={{if @hidden 'hide-xs-up'}}
      for={{this.id}}
    >
      {{@label}}
    </label>
  {{/if}}

  {{yield this.id}}
</div>
