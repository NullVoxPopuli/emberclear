<Pod::Chat::ChatHistory
  class='flex-grow flex-column'
  @messages={{this.messages}}
  @to={{this.model.targetChannel}}/>

<Pod::Chat::ChatEntry class='p-l-md p-r-md' @to={{this.model.targetChannel}} />
